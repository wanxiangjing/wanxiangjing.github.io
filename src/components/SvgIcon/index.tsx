// import React from 'react';

// interface SvgIconProps {
//   name: typeof import('virtual:svg-icons-names')['iconNames'][number]; // 对应 SVG 文件名
//   className?: string;
//   style?: React.CSSProperties;
// }

// const SvgIcon: React.FC<SvgIconProps> = ({ name, className, style }) => {
//   return (
//     <svg className={className} style={{ width:'1rem',height:'1rem',...style,}}>
//       <use href={`#icon-${name}`} />
//     </svg>
//   );
// };

// export default SvgIcon;